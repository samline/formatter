// Root entrypoint — the public surface of `@samline/formatter`.
//
// Today this is a thin re-export of the vanilla surface. Add framework
// adapters or extra subpaths here ONLY when there is a real idiomatic
// use case (see the package-replication guide: do not force wrappers).

export * from './vanilla'